#include "main.h"

/**
 * _abs - gets the absolute value
 *
 * Description: calculates the absolute value of
 * number
 *
 * @n: number whose absolute is attained
 *
 * Return: n's absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}
}

